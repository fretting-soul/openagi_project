import React from 'react';

const ConfigPanel = () => (
    <aside>
        <h2>Configuration Panel</h2>
        <p>Settings and configurations go here.</p>
    </aside>
);

export default ConfigPanel;
